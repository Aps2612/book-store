<h2><%= book.title %></h2>
<img width="100" height="150" src="<%= book.coverImagePath %>">
<div>Author: <%= book.author.name %></div>
<div>Publish Date: <%= book.publishDate.toDateString() %></div>
<div>Page Count: <%= book.pageCount %></div>
<div>Description: <%= book.description %></div>

<a href="/books/<%= book.id %>/edit">Edit</a>
<%- include('../partials/deleteForm', { url: `/books/${book.id}` }) %>
<a href="/authors/<%= book.author.id %>">View Author</a>


<% //this contains all the information about about the book %>
<% //book.coverImagePath is a function defined in book model %>
<% //reason we can aceess tha name of author because of populate which get all info about author efore it returns a book %>
<% //it makes the author a object which in other case would just be an id %>
<% // toDateString will convert in into correct format  which is date only string %>
